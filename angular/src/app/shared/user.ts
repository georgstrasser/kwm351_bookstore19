export class User {
    constructor(
        id: number,
        name: string,
        email: string,
        firstName: string,
        lastName: string,
        address: string,
        isAdmin: boolean
    ){}
}